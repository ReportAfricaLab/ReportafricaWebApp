import { authorSchema } from './author';
import { categorySchema } from './category';
import { articleSchema } from './article';

export const schemaTypes = [authorSchema, categorySchema, articleSchema];
